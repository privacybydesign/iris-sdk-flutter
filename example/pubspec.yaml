name: iris_sdk_flutter_example
description: Minimal host app for iris_sdk_flutter — proves the package builds and links on both platforms.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  iris_sdk_flutter:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
